spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dropdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: drop
    password: drop
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
